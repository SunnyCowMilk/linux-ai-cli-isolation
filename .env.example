# ========================================================
# Linux AI CLI Isolation - Configuration Template
# ========================================================
# 使用说明:
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 填写你的 API Key 和配置模式
# 3. 运行: ./setup.sh
# ========================================================

# --- [通用设置] ---
# Conda 环境名称 (仅 isolated 模式需要)
CONDA_ENV_NAME=ai_cli_env

# 是否使用国内镜像 (true/false)
USE_CN_MIRROR=true

# 代理设置 (如需代理，填写地址，例如: http://127.0.0.1:7890)
PROXY_URL=

# ========================================================
# 配置模式说明:
#   isolated - 项目级隔离 (需要 Conda，配置存储在项目目录)
#   global   - 全局配置 (配置存储在用户主目录)
#   disabled - 不配置该服务
# ========================================================

# --- [Claude Code 配置] ---
# 配置模式: isolated/global/disabled
CLAUDE_MODE=isolated
# API 地址 (默认: https://api.anthropic.com)
CLAUDE_URL=
# API Key (必填，除非 disabled)
CLAUDE_KEY=
# 主模型
CLAUDE_MODEL=claude-opus-4-5-20251101-thinking
# 快速模型
CLAUDE_SMALL_MODEL=claude-haiku-4-5-20251001

# --- [Gemini CLI 配置] ---
# 配置模式: isolated/global/disabled
GEMINI_MODE=isolated
# API 地址 (默认: https://generativelanguage.googleapis.com)
GEMINI_URL=
# API Key (必填，除非 disabled)
GEMINI_KEY=
# 模型
GEMINI_MODEL=gemini-3-pro-preview

# --- [Codex CLI 配置] ---
# 配置模式: global/disabled (Codex 不支持项目级隔离)
CODEX_MODE=global
# API 地址 (默认: https://api.openai.com/v1，第三方服务填写对应地址)
CODEX_URL=
# API Key (必填，除非 disabled)
CODEX_KEY=
# 模型
CODEX_MODEL=gpt-5.1-codex-max
# 推理等级: low/medium/high
CODEX_REASONING_EFFORT=medium
# Wire API: responses/chat
CODEX_WIRE_API=responses
# 网络访问: enabled/disabled
CODEX_NETWORK_ACCESS=enabled
# 禁用响应存储: true/false
CODEX_DISABLE_RESPONSE_STORAGE=true
