# ========================================================
# Linux AI CLI Isolation - 配置文件
# ========================================================
# 使用说明: cp .env.example .env && nano .env && ./setup.sh
# 详细文档: 见 README.md
# ========================================================

# --- 通用设置 ---
CONDA_ENV_NAME=ai_cli_env       # Conda 环境名（可用已有环境或新建）
USE_CN_MIRROR=true              # 使用国内镜像加速
PROXY_URL=                      # 代理地址，如 http://127.0.0.1:7890

# --- 配置模式 ---
# isolated = 项目级隔离（需 Conda）
# global   = 全局配置（自动配置 shell）
# disabled = 不安装

# --- Claude Code ---
CLAUDE_MODE=isolated            # isolated/global/disabled
CLAUDE_URL=                     # API 地址，留空使用官方
CLAUDE_KEY=                     # API 密钥（必填）
CLAUDE_MODEL=claude-opus-4-5-20251101-thinking
CLAUDE_SMALL_MODEL=claude-haiku-4-5-20251001

# --- Gemini CLI ---
GEMINI_MODE=isolated            # isolated/global/disabled
GEMINI_URL=                     # API 地址，留空使用官方
GEMINI_KEY=                     # API 密钥（必填）
GEMINI_MODEL=gemini-2.5-pro

# --- Codex CLI ---
CODEX_MODE=global               # global/disabled（不支持 isolated）
CODEX_URL=                      # API 地址，留空使用官方（第三方需加 /v1）
CODEX_KEY=                      # API 密钥（必填）
CODEX_MODEL=gpt-5.1-codex-max
CODEX_REASONING_EFFORT=medium   # low/medium/high 推理深度
CODEX_WIRE_API=responses        # responses/chat 通信协议
CODEX_NETWORK_ACCESS=enabled    # enabled/disabled 网络访问
CODEX_DISABLE_RESPONSE_STORAGE=true  # 禁用响应存储保护隐私
